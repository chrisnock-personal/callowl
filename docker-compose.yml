# ─────────────────────────────────────────────────────────────────────────────
# CallOwl — local / home-lab stack
#
# Three services: PostgreSQL, the Node/Express API, and an nginx-served React UI.
# A multi-service compose (rather than a single supervisord container) keeps each
# process independently inspectable — handy while prototyping. Works with both
# `docker compose` and `podman-compose`.
#
#   cp .env.example .env      # adjust credentials
#   podman-compose up --build # or: docker compose up --build
#
# Then open the UI at http://localhost:${HTTP_PORT} (default 8080).
# ─────────────────────────────────────────────────────────────────────────────
# Pinned rather than left to default to the directory basename, so the
# project identity (and therefore image/network names) doesn't shift if this
# directory is ever moved or renamed again.
name: callowl

services:
  db:
    build:
      context: ./postgres
    container_name: callowl-db
    depends_on:
      - minio
    # Enables continuous WAL archiving to MinIO via pgBackRest (point-in-time
    # recovery — see DISASTER_RECOVERY.md and postgres/pgbackrest.conf).
    # archive_timeout forces a WAL segment switch at least this often even
    # under low write volume — otherwise a quiet period leaves the actual
    # PITR granularity far coarser than the 60s this implies.
    command: >
      postgres
      -c archive_mode=on
      -c "archive_command=pgbackrest --stanza=callowl archive-push %p"
      -c archive_timeout=60
    environment:
      POSTGRES_DB: "${PGDATABASE:-callowl}"
      POSTGRES_USER: "${PGUSER:-callowl}"
      POSTGRES_PASSWORD: "${PGPASSWORD:-callowl_pass}"
      # pgBackRest reads these rather than pgbackrest.conf, so the secret
      # values stay out of the baked image — see postgres/pgbackrest.conf.
      PGBACKREST_REPO1_S3_KEY: "${MINIO_ROOT_USER:-opencdr-minio}"
      PGBACKREST_REPO1_S3_KEY_SECRET: "${MINIO_ROOT_PASSWORD:-change_me_minio}"
      PGBACKREST_REPO1_S3_BUCKET: "${MINIO_BUCKET:-callowl-pitr}"
      # Connects as $PGUSER, not the container's default OS user (root) —
      # POSTGRES_USER is the only superuser role that exists (the official
      # image doesn't also create a "postgres" role once it's renamed), and
      # there's no "root" role at all, so an unqualified connection fails
      # auth with "role \"root\" does not exist" before trust/peer rules
      # even come into play.
      PGBACKREST_PG1_USER: "${PGUSER:-callowl}"
      # How often docker-entrypoint-wrapper.sh's background loop takes a new
      # pgBackRest base backup — separate from BACKUP_INTERVAL_HOURS (the
      # existing pg_dump service), and from WAL archiving itself, which is
      # continuous/automatic via archive_command above, not on this schedule.
      PITR_BACKUP_INTERVAL_HOURS: "${PITR_BACKUP_INTERVAL_HOURS:-24}"
    volumes:
      - pgdata:/var/lib/postgresql/data
      # Same host ./backups directory backend/backup already mount — lets
      # the pgBackRest loop write .pitr-last-attempt (base-backup status,
      # read by GET /admin/backups) next to pg_dump's own .last-attempt.
      - ./backups:/backups:z
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PGUSER:-callowl} -d ${PGDATABASE:-callowl}"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    container_name: callowl-backend
    depends_on:
      db:
        condition: service_healthy
    environment:
      PORT: "3001"
      NODE_ENV: "${NODE_ENV:-production}"
      PGHOST: db
      PGPORT: "5432"
      PGDATABASE: "${PGDATABASE:-callowl}"
      PGUSER: "${PGUSER:-callowl}"
      PGPASSWORD: "${PGPASSWORD:-callowl_pass}"
      PGSSL: "false"
      # "*" is fine for a local lab; set to your real origin(s) once this is
      # publicly reachable, as defense in depth (session auth doesn't rely on
      # CORS for protection — cookies aren't sent cross-origin regardless,
      # since this isn't configured with credentials: true).
      CORS_ORIGIN: "${CORS_ORIGIN:-*}"
      SEED_EXAMPLES: "${SEED_EXAMPLES:-false}"
      # Set to require a key on POST /calls/ingest. Leave unset for open ingest.
      INGEST_API_KEY: "${INGEST_API_KEY:-}"
      # Set to require a key on the admin backup/restore actions (not the
      # read-only list). Strongly recommended once exposed beyond a local lab.
      ADMIN_API_KEY: "${ADMIN_API_KEY:-}"
      BACKUPS_DIR: /app/backups
      BACKUP_RETENTION_DAYS: "${BACKUP_RETENTION_DAYS:-14}"
      BACKUP_INTERVAL_HOURS: "${BACKUP_INTERVAL_HOURS:-24}"
      # Same var already passed to `db` (its pgBackRest loop's own schedule)
      # — needed here too so GET /admin/backups can compute base-backup
      # staleness for the dashboard's PITR health warning.
      PITR_BACKUP_INTERVAL_HOURS: "${PITR_BACKUP_INTERVAL_HOURS:-24}"
      # Read here only to report "configured?" + interval on GET /admin/backups
      # — the actual sync credentials live on the `offsite-backup` service.
      OFFSITE_S3_ENDPOINT: "${OFFSITE_S3_ENDPOINT:-}"
      OFFSITE_SYNC_INTERVAL_HOURS: "${OFFSITE_SYNC_INTERVAL_HOURS:-24}"
      # Dashboard/API login — always required, no open mode. Bootstrap admin
      # is created on first boot if no users exist yet; see .env.example.
      BOOTSTRAP_ADMIN_USERNAME: "${BOOTSTRAP_ADMIN_USERNAME:-admin}"
      BOOTSTRAP_ADMIN_PASSWORD: "${BOOTSTRAP_ADMIN_PASSWORD:-}"
      # Defaults to true here (unlike the code-level default in
      # backend/src/config/index.ts) since the frontend nginx container
      # terminates HTTPS by default — see docker-compose.yml's `frontend`
      # service and frontend/docker-entrypoint.sh.
      COOKIE_SECURE: "${COOKIE_SECURE:-true}"
      # How long audit_log entries are kept (pruned on boot and daily).
      AUDIT_LOG_RETENTION_DAYS: "${AUDIT_LOG_RETENTION_DAYS:-90}"
      # Only required if a remote source is configured (⋯ menu → Remote
      # sources) — encrypts that source's own credential at rest.
      REMOTE_SOURCE_ENC_KEY: "${REMOTE_SOURCE_ENC_KEY:-}"
      # Set only during a rotation window — see SECRETS_ROTATION.md.
      REMOTE_SOURCE_ENC_KEY_PREVIOUS: "${REMOTE_SOURCE_ENC_KEY_PREVIOUS:-}"
      REMOTE_SOURCE_REJECTS_RETENTION_DAYS: "${REMOTE_SOURCE_REJECTS_RETENTION_DAYS:-90}"
      # Per-account login lockout, independent of the per-IP rate limit on
      # POST /auth/login.
      LOGIN_LOCKOUT_THRESHOLD: "${LOGIN_LOCKOUT_THRESHOLD:-5}"
      LOGIN_LOCKOUT_DURATION_MINUTES: "${LOGIN_LOCKOUT_DURATION_MINUTES:-15}"
      # Only required if a user enables MFA (⋯ menu → Security) — encrypts
      # that user's TOTP secret at rest.
      MFA_ENC_KEY: "${MFA_ENC_KEY:-}"
      # Set only during a rotation window — see SECRETS_ROTATION.md.
      MFA_ENC_KEY_PREVIOUS: "${MFA_ENC_KEY_PREVIOUS:-}"
      # Shared with `frontend`'s /etc/nginx/certs (same `certs` volume,
      # different mount point) so GET/POST /admin/tls can read/replace the
      # certificate nginx serves — see backend/src/db/tlsCert.ts and
      # frontend/cert-watcher.sh.
      TLS_CERTS_DIR: /app/certs
      # Outbound alerting on backup/PITR/off-host-sync staleness and
      # remote-source poll failures (services/alertService.ts) — left unset,
      # the scheduled check still runs but never actually posts anywhere.
      ALERT_WEBHOOK_URL: "${ALERT_WEBHOOK_URL:-}"
      ALERT_COOLDOWN_HOURS: "${ALERT_COOLDOWN_HOURS:-6}"
      ALERT_CHECK_INTERVAL_MINUTES: "${ALERT_CHECK_INTERVAL_MINUTES:-5}"
    volumes:
      # Read-write: the on-demand backup/restore endpoints write and read here,
      # alongside the scheduled `backup` service. :z (shared label) relabels
      # the host dir for SELinux (podman) since both services mount it.
      - ./backups:/app/backups:z
      - certs:/app/certs
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: callowl-frontend
    depends_on:
      - backend
    ports:
      - "${HTTP_PORT:-8080}:80"
      - "${HTTPS_PORT:-8443}:443"
    environment:
      # Also the compose port mapping above — passed through here too so
      # nginx.conf's 80→443 redirect (rendered by docker-entrypoint.sh) knows
      # the real external port to send a browser back to.
      HTTPS_PORT: "${HTTPS_PORT:-8443}"
      # Optional SAN entries for the self-signed default cert
      # (docker-entrypoint.sh) — only needed if you reach this host by a
      # name/IP a browser needs the cert to match; the default always covers
      # localhost/127.0.0.1.
      CALLOWL_HOST_NAME: "${CALLOWL_HOST_NAME:-}"
      CALLOWL_HOST_IP: "${CALLOWL_HOST_IP:-}"
    volumes:
      # Self-signed cert (or a real one you've mounted over it) — must
      # persist across container recreates, or every redeploy silently mints
      # a new self-signed cert (see docker-entrypoint.sh).
      - certs:/etc/nginx/certs
    restart: unless-stopped

  # Self-hosted S3-compatible object storage — the pgBackRest WAL-archiving
  # target for point-in-time recovery (see db's command/environment below
  # and DISASTER_RECOVERY.md). Chosen over SFTP: neither pgBackRest nor
  # WAL-G support SFTP at all, since continuous WAL shipping needs the
  # ordered, retry-safe delivery an object-storage API gives for free.
  # Internal-only (no host port) — same posture as `db`; reach the console
  # via `podman exec` or a temporary port-forward if you ever need it.
  #
  # Custom-built (see minio/Dockerfile) to bake in a self-signed TLS cert —
  # pgBackRest's S3 driver always speaks TLS (repo1-s3-verify-tls=n in
  # postgres/pgbackrest.conf only skips certificate *verification*, it
  # doesn't drop encryption), so MinIO has to actually terminate TLS or
  # every pgBackRest connection fails with "TLS error ... wrong version
  # number" — confirmed the hard way, not assumed up front.
  minio:
    build:
      context: ./minio
    container_name: callowl-minio
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:-opencdr-minio}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:-change_me_minio}"
    volumes:
      - miniodata:/data
    command: server /data --console-address ":9001"
    restart: unless-stopped

  # One-shot: creates the PITR bucket, then exits. MinIO doesn't auto-create
  # buckets, and `db`'s own pgbackrest loop retries stanza-create until the
  # bucket exists, so this doesn't need to be perfectly ordered before `db`
  # starts — just needs to succeed eventually.
  minio-init:
    image: minio/mc:latest
    container_name: callowl-minio-init
    depends_on:
      - minio
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:-opencdr-minio}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:-change_me_minio}"
      MINIO_BUCKET: "${MINIO_BUCKET:-callowl-pitr}"
    entrypoint:
      - /bin/sh
      - -c
      - |
        until mc alias set --insecure local https://minio:9000 "$$MINIO_ROOT_USER" "$$MINIO_ROOT_PASSWORD"; do
          echo "[minio-init] minio not ready, retrying in 3s..."
          sleep 3
        done
        mc mb --insecure --ignore-existing "local/$$MINIO_BUCKET"
        echo "[minio-init] bucket $$MINIO_BUCKET ready"
    restart: "no"

  # Scheduled pg_dump on a loop (no cron daemon needed) + retention pruning.
  # postgres:16-alpine already ships pg_dump/pg_restore, so it doubles as the
  # image for one-off manual restores: see scripts/restore.sh. Separate from
  # (and unaffected by) db's own pgBackRest/PITR setup below — a portable
  # logical dump has real value PITR doesn't replace, see DISASTER_RECOVERY.md.
  backup:
    image: postgres:16-alpine
    container_name: callowl-backup
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGHOST: db
      PGPORT: "5432"
      PGDATABASE: "${PGDATABASE:-callowl}"
      PGUSER: "${PGUSER:-callowl}"
      PGPASSWORD: "${PGPASSWORD:-callowl_pass}"
      BACKUP_DIR: /backups
      BACKUP_RETENTION_DAYS: "${BACKUP_RETENTION_DAYS:-14}"
      BACKUP_INTERVAL_HOURS: "${BACKUP_INTERVAL_HOURS:-24}"
    volumes:
      - ./backups:/backups:z
      - ./scripts:/scripts:ro,Z
    entrypoint:
      - /bin/sh
      - -c
      - |
        while true; do
          sh /scripts/backup.sh || echo "[backup] run failed, will retry next interval"
          sleep "$$(( BACKUP_INTERVAL_HOURS * 3600 ))"
        done
    restart: unless-stopped

  # Off-host copy of both backup artifacts — pg_dump dumps in ./backups AND
  # the pgBackRest/PITR repo in `minio` — to a separate S3-compatible target
  # you control (OFFSITE_S3_*). Closes the gap DISASTER_RECOVERY.md's
  # Scenario B calls out: neither artifact survives losing this host on its
  # own today, since both live on it. Reuses minio/mc (already the image
  # `minio-init` uses above) rather than adding a new tool for the same job.
  # No-ops (idles, re-checking once per interval) until OFFSITE_S3_ENDPOINT
  # is actually set — unlike everything else in this stack, "copy somewhere
  # else" can't be made zero-config, so it stays present but inert rather
  # than failing a fresh `up` with no target configured.
  offsite-backup:
    image: minio/mc:latest
    container_name: callowl-offsite-backup
    depends_on:
      - minio
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:-opencdr-minio}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:-change_me_minio}"
      MINIO_BUCKET: "${MINIO_BUCKET:-callowl-pitr}"
      BACKUP_DIR: /backups
      OFFSITE_S3_ENDPOINT: "${OFFSITE_S3_ENDPOINT:-}"
      OFFSITE_S3_ACCESS_KEY: "${OFFSITE_S3_ACCESS_KEY:-}"
      OFFSITE_S3_SECRET_KEY: "${OFFSITE_S3_SECRET_KEY:-}"
      OFFSITE_S3_BUCKET: "${OFFSITE_S3_BUCKET:-}"
      OFFSITE_S3_INSECURE: "${OFFSITE_S3_INSECURE:-false}"
      OFFSITE_SYNC_INTERVAL_HOURS: "${OFFSITE_SYNC_INTERVAL_HOURS:-24}"
    volumes:
      # Read-write: the script writes its own .offsite-last-attempt status
      # marker here too, same convention as backup/db's own marker files.
      - ./backups:/backups:z
      - ./scripts:/scripts:ro,Z
    entrypoint:
      - /bin/sh
      - -c
      - |
        while true; do
          sh /scripts/offsite-sync.sh || echo "[offsite-backup] run failed, will retry next interval"
          sleep "$$(( OFFSITE_SYNC_INTERVAL_HOURS * 3600 ))"
        done
    restart: unless-stopped

volumes:
  # Pinned to their pre-rebrand physical names — these hold live data created
  # under the "open-cdr-platform" project name, before "name: callowl" above
  # was added. Without an explicit name here, compose would derive
  # "callowl_pgdata"/"callowl_miniodata" instead and silently start the stack
  # on brand-new, empty volumes rather than these.
  pgdata:
    name: open-cdr-platform_pgdata
    driver: local
  miniodata:
    name: open-cdr-platform_miniodata
    driver: local
  certs:
    driver: local
